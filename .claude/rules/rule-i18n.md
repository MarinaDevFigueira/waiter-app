## i18n
- `useTranslation()` — never hardcode visible strings.
- Languages: `pt-BR` (default), `en-US` in `src/shared/translations/`.
- Interpolation: `{{variableName}}`. Language persists to localStorage.