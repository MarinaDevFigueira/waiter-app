## Swiper Navigation Buttons
- RxJS observable for `{ isBeginning, isEnd }`. Flexbox layout (not `position: absolute`).
- `setTimeout` in `onInit`. Conditional: `{shouldShowPrevButton && <button />}`.