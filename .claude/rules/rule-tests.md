## Tests
- Every `src/components/` component must have `__tests__/{name}.spec.js`. Min 3-5 cases.
- Run selective tests after changes: `npm run test -- src/path/to/file.spec.js`
- Full suite (`npm run test`) only before commits/PRs.