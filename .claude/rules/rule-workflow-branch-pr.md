## Workflow: Branch & PR
- NEVER commit directly to `main`. Create branch first.
- Naming: `feat/`, `fix/`, `refactor/`, `style/`, `chore/`, `docs/` + description.
- Always open PR targeting `main` and return URL to user.